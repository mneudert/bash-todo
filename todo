#!/usr/bin/env bash

basedir="$HOME/.todo$(pwd)"

basedir_exists() {
  return $([[ -d "${basedir}" ]])
}

[ ! -z "$1" ] || exit 0

case "${1}${2}" in
  count)
    basedir_exists || { echo 0; exit 0; }
    find "${basedir}" -maxdepth 1 -type f | wc -l
    ;;

  list)
    basedir_exists || { exit 0; }

    todos=( $(find "${basedir}" -maxdepth 1 -type f | sort -n) )

    todo_num=${#todos[@]}
    pad_len="${#todo_num}"
    pad_str="            "

    for i in "${!todos[@]}"; do
        n=$((1 + $i))

        echo -n "${pad_str:0:$(($pad_len - ${#n}))}${n} | "
        echo -n "$(date -d @$(basename ${todos[$i]}) +"%F %T"): "
        cat "${todos[$i]}"
    done
    ;;

  *)
    mkdir -p "${basedir}"
    echo "$@" > "${basedir}/`date +%s`"
    ;;
esac
